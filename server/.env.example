# Copy this file to .env in this folder (server/.env).

PORT=3001
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=cabsonline
# Set DB_SSL=true for remote hosts that require TLS (e.g. uni webdev MySQL).
# DB_SSL=false
